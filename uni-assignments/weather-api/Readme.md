A RESTful API in Flask, which uses a PostgreSQL database to store weather information, and Adminer for database management, all components running in Docker containers.

Server running on port 5000, adminer on 8080, localhost.
